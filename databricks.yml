bundle:
  name: agspotifyazure_dab
  uuid: 6b30c021-2de8-4b86-b884-ae5f4021c996

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405605376541973.13.azuredatabricks.net
      # we don't provide the root path here as every developer has their own root path using their email id, ideally we don't provide it, we provide it in the PROD or QA
    presets:
      source_linked_deployment: false
    variables:
      catalog: spotify_cata
      schema: ${workspace.current_user.short_name}

  prod:
    mode: production
    workspace:
      host: https://adb-7405605376541973.13.azuredatabricks.net
      root_path: /Workspace/Prod/ahampersonal18113@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: spotify_cata
      schema: prod
    permissions:
      - user_name: ahampersonal18113@gmail.com
        level: CAN_MANAGE
